"use client";
import { Question, Topic } from "@/types/topic/topic";
import { QuestionEditor } from "../QuestionEditor";
import { TopicInfoEditor } from "./TopicInfoEditor";
import { QuestionList } from "./QuestionList";
import { Button } from "@/components/ui/button";
export function TopicEditor({ topic, questions, tab, selected, preview, onTab, onTopicChange, onImage, onError, onSelect, onAdd, onDelete, onReorder, onQuestionChange, onSave }: any) { const activeIndex=questions.findIndex((q:Question)=>q.id===selected); const active=questions[activeIndex]; return <><div role="tablist" className="flex gap-4 border-b border-zinc-700"><button role="tab" aria-selected={tab==="topic"} className={`py-2 ${tab==="topic"?"border-b border-zinc-100":"text-zinc-500"}`} onClick={()=>onTab("topic")}>주제</button><button role="tab" aria-selected={tab==="questions"} className={`py-2 ${tab==="questions"?"border-b border-zinc-100":"text-zinc-500"}`} onClick={()=>onTab("questions")}>문제</button></div>{tab==="topic"?<TopicInfoEditor topic={topic} preview={preview} onTopicChange={onTopicChange} onImage={onImage} onError={onError}/>:<div className="flex min-h-80 gap-6"><QuestionList questions={questions} selected={selected} onSelect={onSelect} onAdd={onAdd} onDelete={onDelete} onReorder={onReorder}/>{active&&<QuestionEditor key={active.id} question={active} number={activeIndex+1} onChange={onQuestionChange}/>}</div>}<div className="flex justify-end gap-3"><Button variant="secondary" onClick={()=>onSave(true)}>추가 요청</Button><Button variant="secondary" onClick={()=>onSave(false)}>저장</Button></div></>; }
